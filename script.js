/**
 * O JavaScript ativo do formulário está no final de index.html.
 *
 * Este arquivo foi mantido apenas para não quebrar links antigos. A versão
 * anterior fazia um envio `no-cors` e podia mostrar sucesso mesmo quando o
 * backend recusava o pedido; por isso ela não deve voltar a ser utilizada.
 */
